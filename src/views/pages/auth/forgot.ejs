<div class="w-full max-w-md space-y-8">
  <div class="space-y-2">
    <h1 class="text-3xl sm:text-4xl font-bold text-center">Mot de passe oublié ?</h1>
    <p class="text-center text-muted">Entrez votre email pour réinitialiser votre mot de passe</p>
  </div>

  <form method="POST" action="/auth/forgot" class="space-y-4">
    <div class="space-y-2">
      <label class="block text-muted text-sm font-semibold tracking-wide">Email</label>
      <div class="flex items-center gap-3 bg-[#F4F5F7] rounded-xl px-4 py-3 border border-transparent focus-within:bg-white focus-within:border-border transition">
        <svg class="w-5 h-5 text-muted" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
        </svg>
        <input 
          type="email" 
          name="email" 
          placeholder="prenom@entreprise.com" 
          class="flex-1 bg-transparent outline-none text-primary placeholder:text-muted"
          required
        >
      </div>
    </div>

    <button 
      type="submit" 
      class="w-full bg-primary text-white font-semibold py-3 rounded-xl hover:bg-primary/90 transition"
    >
      Envoyer le lien de réinitialisation
    </button>
  </form>

  <p class="text-center text-muted text-sm">
    <a href="/auth/login" class="text-primary font-semibold hover:underline">← Retour à la connexion</a>
  </p>
</div>

